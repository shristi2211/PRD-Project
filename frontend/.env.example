# Backend API URL — set this in Vercel Environment Variables
# For local development, leave empty (defaults to http://localhost:8080)
# For production (Render), set to your deployed backend URL e.g.:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
